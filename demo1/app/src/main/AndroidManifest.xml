<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AnalysisActivity"></activity>
        <activity android:name=".CallActivity" />
        <activity android:name=".HomeworkDetailActivity" />
        <activity android:name=".AddHomeworkActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UplodaFileActivity" />
        <activity android:name=".CheckDiscussActivity" />
        <activity android:name=".DiscussActivity" />
        <activity android:name=".DrawActivity" />
        <activity android:name=".HomeWorkActivity" />
        <activity android:name=".ClassActivity" />
        <activity android:name=".FileActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>